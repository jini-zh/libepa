defconst(double, alpha);
defconst(double, planck);
defconst(double, barn);
defconst(double, fm);
defconst(double, amu);
defconst(double, infinity);

defvar(int,    print_backtrace);
defvar(double, default_absolute_error);
defvar(double, default_relative_error);
defvar(double, default_error_step);
defvar(size_t, default_integration_limit);
defvar(size_t, default_cquad_integration_limit);
